import { Typography } from "antd";

const Header = () => {
  return (
    <Header className="custom-header">
      <Typography.Text className="header-title">Dashboard</Typography.Text>
    </Header>
  );
};

export default Header;
